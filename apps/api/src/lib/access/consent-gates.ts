import { ApiError } from "../errors/api-error"; import type { PrismaClient } from "@prisma/client";
export async function assertAdvisorCanViewClientAptivio(prisma: PrismaClient, advisorUserId: string, clientId: string) { const assignment = await prisma.advisorClientAssignment.findFirst({ where: { advisorUserId, clientUserId: clientId, status: "ACTIVE" } }); if (!assignment) { throw new ApiError({ statusCode: 403, code: "ADVISOR_NOT_ASSIGNED", message: "Advisor is not assigned to this client." }); } const consent = await prisma.consentRecord.findFirst({ where: { userId: clientId, consentType: "ADVISOR_DATA_SHARING_CONSENT", revokedAt: null }, orderBy: { acceptedAt: "desc" } }); return { assignment, consent, canViewSummary: Boolean(consent) }; }
