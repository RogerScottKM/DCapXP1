export class ApiError extends Error { public statusCode: number; public code: string; public fieldErrors?: Record<string, string>; public retryable?: boolean; constructor(args: { statusCode: number; code: string; message: string; fieldErrors?: Record<string, string>; retryable?: boolean; }) { super(args.message); this.statusCode = args.statusCode; this.code = args.code; this.fieldErrors = args.fieldErrors; this.retryable = args.retryable; } }
