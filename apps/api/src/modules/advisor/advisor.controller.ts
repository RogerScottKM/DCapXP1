import type { Request, Response, NextFunction } from "express"; import { advisorService } from "./advisor.service"; export async function getAdvisorClientAptivioSummary(req: Request, res: Response, next: NextFunction) { try { const advisorUserId = req.auth!.userId; const { clientId } = req.params; const result = await advisorService.getClientAptivioSummary(advisorUserId, clientId); res.json(result); } catch (error) { next(error); } }
